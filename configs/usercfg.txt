# Device-specific settings (preserved across bootloader updates)
# Loaded by config.txt via "include usercfg.txt"

boot_delay=0
arm_control=0x200
kernel=vmlinuz-rpi
initramfs initramfs
gpu_mem=256
dtparam=sd_overclock=100
dtoverlay=vc4-kms-v3d
disable_overscan=1
hdmi_drive=2
dtparam=audio=on
dtparam=krnbt=on

# === PocketTerm35 additions ===
# dtoverlay=waveshare-35dpi-4b   # enabled in config.txt
# dtparam=i2c_arm=on              # enabled in config.txt
